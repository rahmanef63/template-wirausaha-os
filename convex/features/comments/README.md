# comments (convex feature)

Threaded comments — resolvable discussion threads attachable to any entity
(pages, blocks, documents).

**Tables:** `comment_threads`
**Functions:** `query.ts`, `mutation.ts`

Schema composes into the root via `commentsTables` in `_schema.ts`.
Part of Rahman Resources.
