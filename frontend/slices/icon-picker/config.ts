import { defineFeature } from "@/lib/shared/features/defineFeature";

export const iconPickerFeature = defineFeature({
  slug: "icon-picker",
  title: "Icon Picker",
  category: "ui",
  routes: [],
  nav: { label: "Icon Picker", group: "tools", icon: "Smile" },
});
