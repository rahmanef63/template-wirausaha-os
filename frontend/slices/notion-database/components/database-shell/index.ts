/** database-shell — Phase 4 CK-1D port. DB-level wrappers (full-page +
 *  menu). Composes the existing NotionDatabase + RowSelection +
 *  RowPeek primitives. Props-driven — no Convex / store coupling. */

export { DatabaseMenu, type DatabaseMenuProps } from "./DatabaseMenu";
export { DatabasePage, type DatabasePageProps } from "./DatabasePage";
