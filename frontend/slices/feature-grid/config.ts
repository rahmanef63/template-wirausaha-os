import { defineFeature } from "@/lib/shared/features/defineFeature";

export const featureGridFeature = defineFeature({
  slug: "feature-grid",
  title: "Feature Grid",
  category: "ui",
  routes: [],
  nav: { label: "Feature Grid", group: "ui", order: 81 },
});
